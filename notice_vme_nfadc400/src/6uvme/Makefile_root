# Makefile for 6uvme (ROOT Library)
FILENAME = Notice6UVME
LIBname  = libNotice6UVME_root.so

CXX      = g++
ROOTCFLAGS := $(shell root-config --cflags)
ROOTLIBS   := $(shell root-config --libs)

# NKROOT 매크로를 정의하여 C++ ROOT 클래스로 컴파일
CXXFLAGS = -fPIC -O2 -Wall -DNKROOT $(ROOTCFLAGS) -I. -I$(NKHOME)/include
LDFLAGS  = -shared $(ROOTLIBS)

# Dictionary files
DICT      = G__$(FILENAME)
DICTSRC   = $(DICT).cxx
DICTOBJ   = $(DICT).o

all: $(LIBname)

$(LIBname): $(FILENAME).o $(DICTOBJ)
	$(CXX) $(LDFLAGS) -o $@ $^ -L$(NKHOME)/lib -llinux_interface_c
	@echo "Installing $(LIBname) to $(NKHOME)/lib"
	cp $(LIBname) $(NKHOME)/lib/
	cp $(FILENAME).h $(NKHOME)/include/

# ROOT 6 방식: rootcling 사용
$(DICTSRC): $(FILENAME).h LinkDef.h
	rootcling -f $@ -c -I$(NKHOME)/include $^

# 소스 코드가 .c지만 C++ 컴파일러(CXX)로 빌드
%.o: %.c
	$(CXX) $(CXXFLAGS) -c $< -o $@

%.o: %.cxx
	$(CXX) $(CXXFLAGS) -c $< -o $@

clean:
	rm -f *.o *.so $(DICTSRC) $(DICT)*